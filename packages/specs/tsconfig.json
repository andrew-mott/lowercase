{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    // scripts/ runs directly under Node, which needs the .ts in its imports
    "allowImportingTsExtensions": true
  },
  "include": ["src", "tests", "scripts"]
}
